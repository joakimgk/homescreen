import styled from "styled-components"

const Wrapper = styled.div`
    display: flex;
    padding: 2em;
    border: 2px dashed orange;
`;

export const Sensor = () => (
    <Wrapper>
        <b>Sensor</b>
    </Wrapper>
)